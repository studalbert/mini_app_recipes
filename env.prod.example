# Переменные для docker-compose.prod.yml — сама Postgres
# Скопируй в .env (без .example) в корне репозитория и заполни своими значениями
POSTGRES_DB=recipe_db
POSTGRES_USER=recipe_user
POSTGRES_PASSWORD=придумай-длинный-случайный-пароль
